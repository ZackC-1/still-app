// Shared UI configuration constants.

/**
 * The hosted privacy-policy URL, linked in-app (App Store Guideline 5.1.1 also requires it in the
 * App Store Connect metadata). PLACEHOLDER — swap for the final hosted URL before submission; the
 * metadata privacy-policy field must point at the same document.
 */
export const PRIVACY_POLICY_URL = "https://still.app/privacy";
