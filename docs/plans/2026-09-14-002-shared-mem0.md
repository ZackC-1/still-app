# Shared Mem0 for Still

Status: implementation and cross-client verification complete; protected integration pending.

The owner requests recording Still project memories in both Codex and Claude Code. Establish one
scope and instruction-driven retrieval/capture, keeping repository documents authoritative and
credentials in private local configuration. Do not install raw-transcript capture or change app code.

1. Check existing clients and Mem0 account connectivity without printing credentials.
2. Connect missing clients locally; preserve unrelated projects and settings.
3. Define shared read/write scope, checkpoints, privacy boundaries, failed-save behavior and verified
   completion in one protocol referenced by AGENTS.md and CLAUDE.md.
4. Verify Codex-created memory can be read by Claude Code and Claude-created memory by Codex.
5. Record the current release decisions in Mem0, merge reviewed documentation through normal gates,
   and leave root main clean and synchronized.

Acceptance: shared reads include both harnesses; no agent filter hides the other writer; writes use
explicit still-app user/app scope and infer=false; asynchronous success is checked; no credentials
or raw transcripts enter Git or Mem0; failures do not silently change scope or claim success.

Observed setup: Codex Personal/default was already connected, Codex Work lacked Mem0, and Claude
Code had no Mem0 connection. Added Work's private profile entry and Claude's Still-local connection.
Claude MCP health check passed. A proposed global Claude registration was rejected by approval review;
the project-local connection was retained. Separate standalone worktrees need local registration;
connected sessions can continue using their tools while working in worktrees.

Verification: Claude Code retrieved the Codex-created documentation pointer, then saved a scoped
verification memory with SUCCEEDED status; Codex retrieved that Claude-authored memory using the
shared scope. Claude local MCP health check passed. Private client credentials were not printed or
committed. Documentation/link/scope checks passed; required CI gates run on the integration PR. Rollback: revert only this documentation change and remove only
the newly added private client registrations if requested; never bulk-delete shared memories.
